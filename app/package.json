{
  "name": "deepagents-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript POC for the LangChain deepagents library.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@langchain/core": "^1.1.0",
    "@langchain/openai": "^1.4.0",
    "deepagents": "^1.10.2",
    "langchain": "^1.4.0",
    "zod": "^4.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
